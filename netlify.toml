[build]
  # Build command - only copy frontend files (no Python needed)
  command = "bash build.sh"
  
  # Directory to publish (where the built files are)
  publish = "public"

[dev]
  # Local dev settings
  framework = "html"
  command = "echo 'Serving from RecipieGenerator/Template'"
  targetPort = 3000

[[redirects]]
  # SPA redirect - send all 404s to index.html
  from = "/*"
  to = "/index.html"
  status = 200

# Skip installing Python packages - Netlify only needs frontend files
[build.environment]
  SKIP_INSTALL_DEVTOOLS = "true"
  NPM_FLAGS = "--legacy-peer-deps"

# Environment variables for API calls
[context.production.environment]
  REACT_APP_API_URL = "https://food-recipe-api.onrender.com"

[context.preview.environment]
  REACT_APP_API_URL = "https://food-recipe-api.onrender.com"

[context.branch-deploy.environment]
  REACT_APP_API_URL = "https://food-recipe-api.onrender.com"
